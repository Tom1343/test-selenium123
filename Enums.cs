namespace CGI_OUTBOUND_BC_Scenarios.Utilities
{
    public static class Enums
    {
        public enum PropertyType
        {
            Id,
            Name,
            LinkText,
            PartialLinkText,
            ClassName,
            CssName,
            XPath,
            TagName,
            CssSelector
        }
    }
}
